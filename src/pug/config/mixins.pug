
mixin svg(classIco, name, viewBoxStr)
    if (viewBoxStr)
        svg(class= classIco, viewBox=viewBoxStr)
            use(xlink:href='img/svg/symbols.svg#'+ name)
    else
        svg(class= classIco)
            use(xlink:href='img/svg/symbols.svg#'+ name)
    

mixin listItem(text, className, href, name, secClass, viewBoxStr)
    if(href)
        li(class= className+'__item')
            if(secClass)
                #{secClass}
            a(class= className+'__link' href= href)= text
                if(name)
                    +svg(className+'__ico', name, viewBoxStr)
            if(secClass)
                #{secClass}
    else 
        li(class= className+'__item')= text

mixin headerSame()
    .parallax#parallax
        .parallax__layer
            img(src=`img/mountains/originalCompressed/layer10.png`).parallax__layer-img.parallax__layer-img--sky
        - for (var i = 9; i > 0; i--)
            .parallax__layer
                img(src=`img/mountains/originalCompressed/layer${i}.png`).parallax__layer-img

    .menu
        ul.menu__list
            +listItem('Мои работы', 'menu', 'works.html', '', secClass)
            +listItem('Блог', 'menu', 'blog.html', '', secClass)
            +listItem('Обо мне', 'menu', 'about.html', '', secClass)
            +listItem('Авторизация', 'menu', 'index.html', '', secClass)
    .hamburger
        ul.hamburger__list
            li.hamburger__line.hamburger__line--one
            li.hamburger__line.hamburger__line--two
            li.hamburger__line.hamburger__line--three
    ul.social
        +listItem('', 'social', 'https://vk.com/sovofeel','vk')
        +listItem('', 'social', 'https://github.com/sovofeel', 'github')
        +listItem('', 'social', 'https://twitter.com/sovofeel', 'tw')
    .header__triangle-left
    .header__triangle-right
    a.header__arrow-link(href='#')
        +svg('header__arrow-img', 'arrow_down')
 
mixin commentPerson(img, name, prof)
    .comment__person
        .comment__person-avatar
            img(class="comment__person-img" src= img, alt= name)
        .comment__person-desc
            .comment__person-name= name
            .comment__person-prof= prof

mixin contactItem(text, className, href, ico)
    li(class= className+'__item')
        +svg(className+'__ico', ico)
        if(href)
            a(class= className+'__link' href= href)= text
        else
            span(class= className+'__text')= text 

mixin skillProgress(name, pct)
    .skill(data-pct= pct name= name)
        svg(class="skill__svg" viewBox="0 0 220 220" version="1.1" xmlns="http://www.w3.org/2000/svg")
            circle(class="skill__circle" r="90" cx="110" cy="110" fill="transparent" stroke-dasharray="565.48" stroke-dashoffset="0")
            circle(class="skill__circle skill__bar" r="90" cx="110" cy="110" fill="transparent" stroke-dasharray="565.48" stroke-dashoffset="0")
